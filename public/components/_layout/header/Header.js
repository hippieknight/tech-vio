import styled from "@emotion/styled";
import HeadRoom from "react-headroom";

const Header = () => {
  return <HeaderWrap></HeaderWrap>;
};

export default Header;

const HeaderWrap = styled(HeadRoom)``;
